@model IEnumerable<ToDo.Models.Users>

<div class="container p-3">
    <div class="row pt-4">
        <div class="col-6">
            <h2 class="text-primary">Users List</h2>
        </div>
        <div class="col-6 text-right">
            <a asp-controller="UsersManagement" asp-action="Create" class="btn btn-primary">
                Create User
            </a>
        </div>
    </div>

    <br /><br />

    @if (Model.Count() > 0)
    {
        <table class="table table-bordered table-striped" style="width:100%">
            <thead>
                <tr>
                    <th>
                        Id
                    </th>
                    <th>
                        Username
                    </th>
                    <th>
                        Password
                    </th>
                    <th>
                        First name
                    </th>
                    <th>
                        Last name
                    </th>
                    <th>
                        Date of creation
                    </th>
                    <th>
                        Id of creator
                    </th>
                    <th>
                        Date of edit
                    </th>
                    <th>
                        Id of editor
                    </th>
                    <th></th>
                </tr>
            </thead>
            <tbody>
                @foreach (var obj in Model)
                {
                <tr>
                    <td width="20%">@obj.Id</td>
                    <td width="10%">@obj.Username</td>
                    <td width="10%">@obj.Password</td>
                    <td width="10%">@obj.FirstName</td>
                    <td width="10%">@obj.LastName</td>
                    <td width="10%">@obj.DateCreation</td>
                    <td width="10%">@obj.IdCreator</td>
                    <td width="10%">@obj.DateChange</td>
                    <td width="10%">@obj.IdChange</td>
                    <td class="text-center">
                        <div class="w-75 btn-group" role="group">
                            <a asp-controller="UsersManagement" asp-route-Id="@obj.Id" asp-action="Edit" class="btn btn-primary mx-2">
                                Edit
                            </a>
                            <a asp-controller="UsersManagement" asp-route-Id="@obj.Id" asp-action="Delete" class="btn btn-danger mx-2">
                                Delete
                            </a>
                        </div>
                    </td>
                </tr>
                }
            </tbody>
        </table>
    }
    else
    {
        <p> No category exists.</p>
    }

</div>